#ifndef FILTERS_H
#define FILTERS_H

#include <iostream>
#include <opencv2/opencv.hpp>
#include <fstream>

#include<stdio.h>
#include<stdlib.h>
#include<stdbool.h>

using namespace std;
using namespace cv;

void myMeanFilter( InputArray src, OutputArray dst, Size ksize,	Point anchor, int borderType) ;
void myMedianFilter( InputArray src, OutputArray dst, int ksize );
void myGaussianFilter( InputArray src, OutputArray dst, Size ksize, double sigmaX, double sigmaY, int borderType );
void myBilateralFilter( InputArray src, OutputArray dst, int d, double  sigmaColor, double sigmaSpace, int borderType );	

#endif // FILTERS_H
